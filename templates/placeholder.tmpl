<span class="su-placeholder-wrap">
	<su-transclude></su-transclude>
	<span class="su-placeholder su-left" ng-if="position === 'left'">{{placeholder}}</span>
	<span class="su-placeholder su-right" ng-if="position === 'right'">{{placeholder}}</span>
</span>
